meta {
  name: 05-400-invalid-window
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}{{servicePath}}/test-only/overrides/{{overrideZReference}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "reportingWindow": {
      "startDate": "{{reportingWindowOverrideEnd}}",
      "endDate": "{{reportingWindowOverrideStart}}"
    }
  }
}

assert {
  res.status: eq 400
}
